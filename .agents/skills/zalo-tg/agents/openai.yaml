interface:
  display_name: "Zalo Tg"
  short_description: "Repo-specific patterns and workflows for zalo-tg"
  default_prompt: "Use the zalo-tg repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true